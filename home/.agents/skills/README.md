# User Codex Skills

This directory is linked to `~/.agents/skills` for user-authored Codex skills.

Keep only durable, safe-to-sync skill source here. Private generated state,
auth data, plugin caches, and Codex memories belong in the encrypted Codex
state archive created by `bin/codex-backup`.
